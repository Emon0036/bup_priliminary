const Block=({title,children}:{title:string,children:React.ReactNode})=><section className="card"><h2 className="text-xl font-bold">{title}</h2><div className="mt-2 text-slate-700">{children}</div></section>;
export default function Methodology(){return <div className="space-y-4"><p className="label">AI Lab evaluation guide</p><h1 className="text-3xl font-bold">Methodology & limitations</h1><Block title="Simulation workflow">Economic event → environment update → agent observation → decision → risk prediction → saved outcome → next monthly state.</Block><Block title="A* Search">Each location is a grid coordinate. A* uses movement cost 1 and Manhattan distance heuristic to find valid routes around blocked buildings.</Block><Block title="Constraint Satisfaction Problem">Variables are a bounded batch of job-seeking agents. Domains are eligible active jobs. Backtracking with MRV and capacity forward checking enforces skill and vacancy constraints.</Block><Block title="Logistic Regression">The transparent classifier estimates financial stress from expense-to-income ratio, savings coverage, debt ratio, family size, employment and inflation exposure. It reports a probability rather than a policy forecast.</Block><Block title="Synthetic data">All 500 people and calibration values are synthetic demonstrations. Future BBS HIES/LFS/CPI, Bangladesh Bank and World Bank data can calibrate distributions. This is an educational simulation and should not be used as a real economic policy forecasting system.</Block></div>}
