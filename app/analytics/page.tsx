"use client";
import {useEffect,useState} from "react"; import {api} from "@/lib/api"; import {LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer,Legend} from "recharts";
export default function Analytics(){const [data,setData]=useState<any>();useEffect(()=>{api<any>("/api/analytics").then(setData)},[]);return <section><p className="label">Calculated from simulation states</p><h1 className="text-3xl font-bold">Society analytics</h1><div className="card mt-5 h-[380px]">{!data?"Loading chart…":<ResponsiveContainer width="100%" height="100%"><LineChart data={data.series}><XAxis dataKey="month"/><YAxis/><Tooltip/><Legend/><Line type="monotone" dataKey="average_savings" stroke="#087e8b" strokeWidth={3}/><Line type="monotone" dataKey="average_expense" stroke="#ed8a19" strokeWidth={3}/><Line type="monotone" dataKey="at_risk_percentage" stroke="#ca4b38" strokeWidth={3}/></LineChart></ResponsiveContainer>}</div><p className="mt-3 text-sm text-slate-600">Run months in Simulation to add real calculated points. This chart never uses hard-coded scenario results.</p></section>}
