# Calibration data

All initial values are synthetic demonstration parameters, not official Bangladesh statistics. Replace `calibration.json` with verified BBS/HIES/LFS/CPI-derived parameters for research calibration.
