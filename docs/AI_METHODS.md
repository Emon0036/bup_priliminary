# AI methods

`astar.py` implements four-way A* using a Manhattan heuristic. `csp.py` implements bounded (30-agent) job allocation with MRV, backtracking and capacity checks. `risk_model.py` trains a deterministic Logistic Regression model from transparent synthetic labels at build/import time. The behavioral engine follows readable household-budget rules rather than claiming scientific forecasting accuracy.
