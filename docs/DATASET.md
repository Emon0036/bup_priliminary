# Dataset strategy

The default 500-agent population is deterministic synthetic demonstration data (seed 42). `data/calibration.json` exposes ranges and weights. BBS HIES could calibrate household expenses/family size; LFS occupations, skills and employment; CPI price shocks; Bangladesh Bank income/macroeconomic assumptions; World Bank macro context. No value is represented as official data.
